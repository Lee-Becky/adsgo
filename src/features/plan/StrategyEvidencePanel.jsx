import { BookOpen, Check, Database, Flag, Package, Sparkles } from 'lucide-react'
import { useState } from 'react'
import useMarketingOpsStore from '@stores/marketingOpsStore'
import useBrandStore from '@stores/brandStore'

const StrategyEvidencePanel=()=>{const [included,setIncluded]=useState(true),views=useMarketingOpsStore(s=>s.views),actions=useMarketingOpsStore(s=>s.actions),brand=useBrandStore(s=>s.selectedBrand),knowledge=useBrandStore(s=>s.brandKnowledge[brand]||[]);const evidence=[[Database,'数据视图',views[0]?.name||'渠道经营总览'],[Flag,'目标约束','ROAS ≥ 2.0 · CPA ≤ $42'],[Package,'产品上下文','主推产品 · Core Training Set'],[BookOpen,'品牌知识',knowledge[0]?.title||'促销期预算保护规则'],[Check,'历史验证',`${actions.filter(x=>x.status==='verified').length||2} 个有效动作`]];return <section className="rounded-2xl border border-luna-border bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-luna-bg text-luna-violet"><Sparkles size={18}/></span><div><h3 className="font-bold">本周策略依据链</h3><p className="mt-1 text-xs text-neutral-500">说明 Luna 使用了哪些事实、规则和已验证经验</p></div></div><button onClick={()=>setIncluded(!included)} className={`rounded-lg px-3 py-2 text-xs font-semibold ${included?'bg-success-50 text-success-700':'border text-neutral-600'}`}>{included?'已纳入策略':'纳入本周策略'}</button></div><div className="mt-4 grid gap-2 sm:grid-cols-2 xl:grid-cols-5">{evidence.map(([Icon,label,value])=><div key={label} className="rounded-xl bg-neutral-50 p-3"><Icon size={14} className="text-luna-violet"/><p className="mt-2 text-[10px] uppercase tracking-wide text-neutral-400">{label}</p><p className="mt-1 text-xs font-semibold leading-5 text-neutral-700">{value}</p></div>)}</div><div className="mt-4 rounded-xl border border-primary-100 bg-primary-50/50 p-4"><p className="text-xs font-semibold text-primary-800">策略结论</p><p className="mt-1 text-sm leading-6 text-neutral-700">优先保留德国市场高回报广告组，将英国 TikTok 预算下调 15%，并启动两组 UGC 换新测试；高影响动作仍需优化师确认。</p></div></section>}
export default StrategyEvidencePanel
