import { ArrowRight, BookOpen, CheckCircle2, Database, MousePointerClick, Sparkles } from 'lucide-react'
import useMarketingOpsStore from '@stores/marketingOpsStore'

const ActionTracePanel=()=>{const actions=useMarketingOpsStore(s=>s.actions),views=useMarketingOpsStore(s=>s.views),item=actions[0];if(!item)return null;const nodes=[['问题',item.before||'经营指标异常',Database],['Luna 判断',item.skillId?'预算健康检查':'人工判断',Sparkles],['人工决策',`${item.decision||'采纳'}${item.decisionReason?` · ${item.decisionReason}`:''}`,MousePointerClick],['实际动作',item.action,ArrowRight],['效果结果',item.result||`等待 ${item.verificationWindow} 验证`,CheckCircle2]];return <section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-bold">当前动作完整证据链</h3><p className="mt-1 text-xs text-neutral-500">{item.target} · 来源建议 {item.recommendationId||'人工创建'}</p></div><span className="w-fit rounded-full bg-primary-50 px-3 py-1 text-xs font-semibold text-primary-700">{item.status}</span></div><div className="mt-4 grid gap-2 lg:grid-cols-5">{nodes.map(([label,value,Icon],i)=><div key={label} className="relative rounded-xl bg-neutral-50 p-3"><Icon size={14} className="text-luna-violet"/><p className="mt-2 text-[10px] font-bold text-neutral-400">0{i+1} · {label}</p><p className="mt-1 text-xs font-semibold leading-5 text-neutral-700">{value}</p></div>)}</div><div className="mt-3 flex flex-wrap gap-2 text-[10px] text-neutral-500"><span className="rounded-full border px-2 py-1"><Database size={10} className="mr-1 inline"/>{views.find(x=>x.id===item.viewId)?.name||'关联数据视图'}</span><span className="rounded-full border px-2 py-1"><BookOpen size={10} className="mr-1 inline"/>{item.knowledgeIds?.length||0} 条知识依据</span><span className="rounded-full border px-2 py-1">动作 ID：{item.actionId||item.id}</span></div></section>}
export default ActionTracePanel
