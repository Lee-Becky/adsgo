import { useState } from 'react'
import { Activity, ArrowRight, BookOpen, BrainCircuit, CheckCircle2, Database, FileBarChart, History, Sparkles, X } from 'lucide-react'
import useMarketingOpsStore from '@stores/marketingOpsStore'

const relations=[
 { title:'品牌 KPI 与预算红线', source:'品牌管理员维护', type:'人工录入', used:['预算红线检查','每日账户健康检查','周度策略'], references:18, last:'今天 09:00' },
 { title:'历史操作经验', source:'动作效果验证', type:'系统沉淀', used:['Luna 预算建议','效果与复盘'], references:9, last:'昨天 18:00' },
]

const KnowledgeIntelligencePanel=()=>{
 const learning=useMarketingOpsStore(s=>s.learningRecords)
 const [tab,setTab]=useState('relations'),[detail,setDetail]=useState(null)
 return <section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-bold">知识来源与学习记录</h3><p className="mt-1 text-xs text-neutral-500">查看知识从哪里产生、被哪些能力引用，以及系统最近学到了什么</p></div><div className="flex rounded-lg bg-neutral-100 p-1">{[['relations','引用关系'],['learning','品牌学习']].map(([id,label])=><button key={id} onClick={()=>setTab(id)} className={`rounded-md px-3 py-2 text-xs font-semibold ${tab===id?'bg-white shadow-sm':'text-neutral-500'}`}>{label}</button>)}</div></div>{tab==='relations'?<div className="mt-4 grid gap-3 md:grid-cols-2">{relations.map(x=><button key={x.title} onClick={()=>setDetail(x)} className="rounded-xl border p-4 text-left hover:border-primary-200"><div className="flex items-start justify-between"><BookOpen size={17} className="text-primary-600"/><span className="rounded-full bg-neutral-100 px-2 py-1 text-[10px]">引用 {x.references} 次</span></div><h4 className="mt-3 text-sm font-semibold">{x.title}</h4><p className="mt-1 text-xs text-neutral-500">来源：{x.source} · {x.type}</p><div className="mt-3 flex items-center gap-2 text-xs text-primary-700">查看使用关系<ArrowRight size={12}/></div></button>)}</div>:<div className="mt-4 space-y-3">{learning.map(x=><article key={x.id} className="rounded-xl border p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-lg bg-luna-bg text-luna-violet"><BrainCircuit size={16}/></span><div><p className="text-xs text-neutral-400">{x.type} · {x.time}</p><h4 className="text-sm font-semibold">{x.title}</h4></div></div><span className={`rounded-full px-2 py-1 text-[10px] font-semibold ${x.status==='已生效'?'bg-success-50 text-success-700':'bg-warning-50 text-warning-700'}`}>{x.status}</span></div><p className="mt-3 text-sm leading-6 text-neutral-600">{x.detail}</p><p className="mt-2 text-xs text-neutral-400">依据：{x.source}</p><div className="mt-3 flex flex-wrap gap-2">{x.impacts.map(i=><span key={i} className="rounded-full bg-primary-50 px-2 py-1 text-[10px] text-primary-700">影响 {i}</span>)}</div></article>)}</div>}{detail&&<div className="fixed inset-0 z-[9998] grid place-items-center bg-neutral-950/40 p-4" onMouseDown={()=>setDetail(null)}><div onMouseDown={e=>e.stopPropagation()} className="w-full max-w-lg rounded-2xl bg-white p-5 shadow-2xl"><div className="flex justify-between"><div><p className="text-xs text-primary-600">知识使用关系</p><h3 className="mt-1 font-bold">{detail.title}</h3></div><button onClick={()=>setDetail(null)}><X size={18}/></button></div><div className="mt-5 rounded-xl bg-neutral-50 p-4"><p className="text-xs text-neutral-400">来源</p><p className="mt-1 text-sm">{detail.source} · {detail.type}</p></div><div className="mt-4 space-y-2">{detail.used.map((x,i)=><div key={x} className="flex items-center gap-3 rounded-xl border p-3"><span className="grid h-8 w-8 place-items-center rounded-lg bg-primary-50 text-primary-600">{i===0?<Sparkles size={14}/>:i===1?<Activity size={14}/>:<FileBarChart size={14}/>}</span><div><p className="text-sm font-semibold">{x}</p><p className="text-xs text-neutral-400">最近引用：{detail.last}</p></div></div>)}</div></div></div>}</section>
}
export default KnowledgeIntelligencePanel
