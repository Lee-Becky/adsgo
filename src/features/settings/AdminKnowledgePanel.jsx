import { useState } from 'react'
import { BookOpen, Check, PenLine, Plus, Search, X } from 'lucide-react'
import useBrandStore from '@stores/brandStore'

const EMPTY = { level: 'L1', title: '', summary: '', updateFrequency: '季度更新', maintainer: '行业管理员', industry: '电商', status: 'draft' }
const LEVELS = { L1: '行业基础', L2: '投放知识', L3: '竞品动态' }
const LEVEL_STYLE = { L1: 'border-info-200 bg-info-50 text-info-700', L2: 'border-primary-200 bg-primary-50 text-primary-700', L3: 'border-warning-200 bg-warning-50 text-warning-700' }

const KnowledgeAdminModal = ({ item, onClose, onSave }) => {
  const [form, setForm] = useState(item ? { ...item } : EMPTY)
  const [error, setError] = useState('')
  const submit = (event) => { event.preventDefault(); if (!form.title.trim() || !form.summary.trim()) return setError('请填写知识名称和内容摘要'); onSave({ ...form, title: form.title.trim(), summary: form.summary.trim() }) }
  return <div className="fixed inset-0 z-[2000] flex items-center justify-center bg-ink-900/55 p-4 backdrop-blur-sm" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><form onSubmit={submit} className="w-full max-w-xl overflow-hidden rounded-2xl border border-neutral-200 bg-white shadow-2xl"><header className="flex items-center justify-between border-b border-neutral-200 px-6 py-5"><div><p className="text-[11px] font-semibold uppercase tracking-wider text-primary-600">公共知识库</p><h2 className="mt-1 font-heading text-lg font-semibold text-neutral-900">{item ? '编辑知识' : '新增知识'}</h2></div><button type="button" onClick={onClose} aria-label="关闭" className="focus-ring flex h-10 w-10 cursor-pointer items-center justify-center rounded-lg text-neutral-400 hover:bg-neutral-100"><X size={18} /></button></header><div className="max-h-[65vh] space-y-4 overflow-y-auto p-6"><div className="grid gap-4 sm:grid-cols-2"><div><label htmlFor="admin-kb-level" className="mb-1.5 block text-xs font-semibold text-neutral-700">知识层级</label><select id="admin-kb-level" value={form.level} onChange={(event) => setForm({ ...form, level: event.target.value })} className="enhanced-select min-h-11">{Object.entries(LEVELS).map(([level, label]) => <option key={level} value={level}>{level} {label}</option>)}</select></div><div><label htmlFor="admin-kb-industry" className="mb-1.5 block text-xs font-semibold text-neutral-700">适用行业</label><input id="admin-kb-industry" value={form.industry} onChange={(event) => setForm({ ...form, industry: event.target.value })} className="enhanced-input min-h-11" /></div></div><div><label htmlFor="admin-kb-title" className="mb-1.5 block text-xs font-semibold text-neutral-700">知识名称</label><input id="admin-kb-title" value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} className="enhanced-input min-h-11" autoFocus /></div><div><label htmlFor="admin-kb-summary" className="mb-1.5 block text-xs font-semibold text-neutral-700">内容摘要</label><textarea id="admin-kb-summary" value={form.summary} onChange={(event) => setForm({ ...form, summary: event.target.value })} className="enhanced-input min-h-28 resize-none" /></div><div className="grid gap-4 sm:grid-cols-2"><div><label htmlFor="admin-kb-frequency" className="mb-1.5 block text-xs font-semibold text-neutral-700">更新频率</label><input id="admin-kb-frequency" value={form.updateFrequency} onChange={(event) => setForm({ ...form, updateFrequency: event.target.value })} className="enhanced-input min-h-11" /></div><div><label htmlFor="admin-kb-maintainer" className="mb-1.5 block text-xs font-semibold text-neutral-700">维护者</label><input id="admin-kb-maintainer" value={form.maintainer} onChange={(event) => setForm({ ...form, maintainer: event.target.value })} className="enhanced-input min-h-11" /></div></div><div><label htmlFor="admin-kb-status" className="mb-1.5 block text-xs font-semibold text-neutral-700">状态</label><select id="admin-kb-status" value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value })} className="enhanced-select min-h-11"><option value="draft">草稿</option><option value="published">已发布</option><option value="disabled">已停用</option></select></div>{error && <p className="rounded-lg border border-danger-200 bg-danger-50 px-3 py-2 text-xs text-danger-700">{error}</p>}</div><footer className="flex justify-end gap-2 border-t border-neutral-200 bg-neutral-50 px-6 py-4"><button type="button" onClick={onClose} className="focus-ring min-h-11 cursor-pointer rounded-xl border border-neutral-200 bg-white px-4 text-sm font-semibold text-neutral-700">取消</button><button className="focus-ring min-h-11 cursor-pointer rounded-xl bg-primary-600 px-5 text-sm font-semibold text-white">保存知识</button></footer></form></div>
}

const AdminKnowledgePanel = () => {
  const items = useBrandStore((state) => state.industryKnowledge)
  const addItem = useBrandStore((state) => state.addIndustryKnowledge)
  const updateItem = useBrandStore((state) => state.updateIndustryKnowledge)
  const [query, setQuery] = useState('')
  const [editing, setEditing] = useState(null)
  const [open, setOpen] = useState(false)
  const [notice, setNotice] = useState('')
  const filtered = items.filter((item) => `${item.title} ${item.summary} ${item.level} ${item.industry}`.toLowerCase().includes(query.trim().toLowerCase()))
  const save = (form) => { editing ? updateItem(editing.id, form) : addItem(form); setNotice(editing ? '公共知识已更新' : '公共知识已创建'); setOpen(false); setEditing(null) }
  return <div className="space-y-4">{notice && <div className="flex items-center justify-between rounded-xl border border-success-200 bg-success-50 px-4 py-3 text-xs text-success-700"><span className="flex items-center gap-2"><Check size={14} />{notice}</span><button onClick={() => setNotice('')} aria-label="关闭提示"><X size={14} /></button></div>}<div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="relative w-full max-w-sm"><Search size={16} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-neutral-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} aria-label="搜索公共知识" className="enhanced-input min-h-11 pl-9" placeholder="搜索层级、行业或知识" /></div><button onClick={() => { setEditing(null); setOpen(true) }} className="focus-ring inline-flex min-h-11 cursor-pointer items-center justify-center gap-2 rounded-xl bg-primary-600 px-4 text-sm font-semibold text-white"><Plus size={17} />新增公共知识</button></div><div className="grid gap-3 md:grid-cols-2">{filtered.map((item) => <article key={item.id} className="rounded-xl border border-neutral-200 bg-white p-4"><div className="flex items-start justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-primary-50 text-primary-700"><BookOpen size={18} /></span><button onClick={() => { setEditing(item); setOpen(true) }} aria-label={`编辑${item.title}`} className="focus-ring flex h-10 w-10 cursor-pointer items-center justify-center rounded-lg text-neutral-400 hover:bg-neutral-100"><PenLine size={16} /></button></div><div className="mt-3 flex flex-wrap items-center gap-2"><h3 className="font-heading text-sm font-semibold text-neutral-900">{item.title}</h3><span className={`rounded-full border px-2 py-0.5 text-[10px] font-semibold ${LEVEL_STYLE[item.level]}`}>{item.level} {LEVELS[item.level]}</span></div><p className="mt-2 text-xs leading-relaxed text-neutral-500">{item.summary}</p><div className="mt-4 flex flex-wrap gap-2 border-t border-neutral-100 pt-3 text-[10px] text-neutral-500"><span>{item.industry}</span><span>·</span><span>{item.updateFrequency}</span><span>·</span><span>{item.maintainer}</span></div></article>)}</div>{open && <KnowledgeAdminModal item={editing} onClose={() => { setOpen(false); setEditing(null) }} onSave={save} />}</div>
}

export default AdminKnowledgePanel
