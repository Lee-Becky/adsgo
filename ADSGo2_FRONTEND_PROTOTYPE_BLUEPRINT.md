# AdsGo 2.0 前端原型蓝图

## 项目目标

本项目不做真实后端、真实广告平台 API、权限系统或真实调度。目标是通过前端页面、模拟数据和可点击交互，把 AdsGo 2.0 的产品逻辑讲清楚：优化师如何用 GUI 快速管理广告，Luna 如何把自然语言结果回显到对应功能位，人如何确认、编辑、忽略并把经验反馈给 Luna。

核心原则是 `GUI-first, Chat-assisted`。Chat 用于表达意图、生成初始方案、解释原因和追问人工判断；GUI 用于批量筛选、层级浏览、字段编辑、素材预览、预算 diff、执行确认、状态追踪和复盘。

## 一级信息架构

一级菜单沿用产品结构图中的业务路径，不改成抽象工作模式：

- `Luna Chat`：自然语言入口、跨模块解释、客户 QA、人工反馈追问。
- `数据报告与洞察`：今日简报、每日表现数据表、客户可读报告。
- `媒体计划与策略循环`：Media Plan、Strategy Cycle、PDCA、模拟定时任务。
- `广告创编与批量发布`：Bulk Launch、AI 推荐广告结构、草稿预览、发布前检查。
- `广告管理`：单一菜单，内部用 `Campaign / AdSet / Ad` 三个 Tab 管理不同层级。
- `草稿与 AI 推荐预览`：AI 产物进入 GUI 后的结构树、字段编辑和素材预览。
- `创意库`：创意资产、素材表现、媒体平台创意编号映射。
- `核心数据趋势及洞察`：账户、受众、页面、创意深度分析。
- `品牌配置中心`：品牌、账号、目标、数据集、Skills、模拟风控告警。
- `用户管理后台`：多客户、多 workspace、个人统计。

## Demo 剧本

### 剧本 1：美国市场 ROAS 连续下滑

1. 品牌目标设为 `Purchase ROAS >= 2.4`，美国为重点市场。
2. 核心趋势显示 US ROAS 从 `2.6` 降到 `1.8`。
3. 用户在 Luna 问“分析美国过去7天 ROAS 衰退原因”。
4. 广告管理的 Campaign Tab 出现 Luna 建议列，标记 3 个异常 campaign。
5. 用户在 GUI 中查看证据、预算 diff 和风险，采纳 2 条、手动修改 1 条。
6. Luna 在修改位置追问原因，并把回答沉淀为品牌记忆。
7. 策略循环生成观察任务，日报生成客户可读解释。

### 剧本 2：素材疲劳与新创意补充

1. 创意库显示 3 个核心素材频次上升、CTR 下滑。
2. Insight 的创意散点图标记高花费低 CTR 素材。
3. Luna 生成新素材方向和广告结构。
4. 草稿中心展示 Campaign -> AdSet -> Ad 结构树和素材预览。
5. Bulk Launch 做发布前 QA，广告管理回显 scheduled 状态。

### 剧本 3：新客户首月启动

1. 用户管理后台打开新客户 workspace。
2. 品牌配置中心填写行业、市场、目标、账号、数据集和竞品。
3. Media Plan 生成月度计划和 W1-W4 周任务。
4. 广告创编生成首批结构，广告管理展示投放层级。
5. 数据报告与洞察生成客户可读日报。

## 统一模拟数据要求

- 同一品牌、campaign、adset、ad、creative id 必须跨页面一致。
- 同一事件状态必须跨模块一致，例如 `suggested`、`accepted`、`edited`、`scheduled`、`done`。
- 指标要有自然波动，避免过于整齐的假数据。
- 每条 Luna 建议都必须能追溯到模拟数据来源。
- 自动执行、权限、审计、调度都只作为前端状态呈现，不接真实能力。

## 广告管理 GUI 规范

广告管理是第一阶段核心原型，必须是一个页面内的三层级工作台：

- 顶部：品牌、平台、时间窗、目标指标、数据同步状态。
- 层级 Tab：`Campaign`、`AdSet`、`Ad`，切换不离开当前菜单上下文。
- 统一筛选条：状态、市场、目标、异常类型、Luna 建议、素材类型。
- 表格：固定名称列和关键指标列，支持排序、列配置、批量选择。
- Luna 建议列：展示 Increase / Decrease / Maintain / Review、原因摘要和风险等级。
- 详情抽屉：趋势、关联层级、证据栈、before/after diff、模拟审计时间线。
- 底部批量操作栏：仅在选中对象后出现，展示影响范围和预算变化。
- Memory Prompt：当用户行为与 Luna 建议不一致时内联出现，不使用打断式弹窗。

## 视觉方向

采用冷静、精确、低噪声的数据操作系统风格：

- Slate/Zinc 中性底，单一低饱和 accent。
- 避免紫蓝 AI 光效、霓虹、三等分泛卡片和无意义渐变。
- 指标、预算、ROAS 使用等宽数字。
- 高频管理页使用紧凑表格、分组线、sticky header、side panel、split view。
- 动效只服务于状态变化、diff 展开、抽屉打开和任务流转，控制在 150-300ms。
- 不使用 emoji 图标；交互元素必须有 hover、focus、active、loading、empty、error 状态。
